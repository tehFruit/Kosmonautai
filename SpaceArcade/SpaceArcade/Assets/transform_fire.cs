using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class transform_fire : MonoBehaviour
{
    public Transform ene;
    public Transform firePoint;
    //private float x = ene.position.x;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        //Vector3 temp = new Vector3(x + 5.5, ene.position.y, ene.position.z);
        //myGameObject.transform.position += temp;

        //firePoint.position.x = ene.position.x + 5.5;
        //firePoint.position.y = ene.position.y ;
        //firePoint.position.z = ene.position.z ;
    }
}
